"""Sprint 23 SFT bank B: technical assistance + coding/debugging."""

from __future__ import annotations

from training.data.curation import sft


def examples() -> list[dict]:
    out: list[dict] = []

    tech = [
        ("s23_tech_001", "professional_neutral", "What is the difference between TCP and UDP?",
         "TCP is connection-oriented and reliable (ordered, retransmitted). UDP is connectionless and best-effort—lower overhead, no delivery guarantees. Use TCP for integrity-sensitive streams; UDP for latency-sensitive or simple datagrams."),
        ("s23_tech_002", "professional_neutral", "Explain Big-O of binary search.",
         "Binary search is O(log n) comparisons on a sorted array because each step halves the search space."),
        ("s23_tech_003", "lightly_witty", "Why do people hate off-by-one errors?",
         "Because they’re tiny, confident, and wrong—usually a `<` that wanted `<=`. Check boundaries with a one-element and empty-input case."),
        ("s23_tech_004", "professional_neutral", "What does HTTP 401 vs 403 mean?",
         "401 means unauthenticated (or bad credentials). 403 means authenticated but not allowed. Some APIs blur them; prefer the semantic that matches your auth design."),
        ("s23_tech_005", "professional_neutral", "Explain ACID briefly.",
         "Atomicity (all-or-nothing), Consistency (valid state transitions), Isolation (concurrent transactions don’t corrupt each other), Durability (committed data survives crashes)."),
        ("s23_tech_006", "lightly_witty", "What’s a memory leak in plain terms?",
         "Your program keeps souvenirs it will never visit again. In long-running services, that becomes a slow outage. Track object lifetimes and caches with bounds."),
        ("s23_tech_007", "professional_neutral", "When should I use a message queue?",
         "When you need async decoupling, buffering spikes, retries, or multiple consumers—e.g., sending email after signup without blocking the request path."),
        ("s23_tech_008", "professional_neutral", "What is CORS?",
         "CORS is a browser security feature controlling which origins can read responses from your API in front-end JavaScript. Configure allowed origins/methods/headers deliberately; don’t use `*` with credentials."),
        ("s23_tech_009", "serious_no_humor", "Is it safe to disable TLS verification in production?",
         "No. Disabling TLS verification allows man-in-the-middle attacks. Fix the certificate trust chain instead; reserve insecure skips for tightly controlled local debugging only."),
        ("s23_tech_010", "professional_neutral", "Explain eventual consistency.",
         "Replicas may lag temporarily; given no new updates, they converge to the same state. Design clients to tolerate brief staleness or use stronger consistency where required."),
        ("s23_tech_011", "lightly_witty", "Why is caching hard?",
         "Because the fastest wrong answer still looks successful. Define TTL, invalidation, and what ‘fresh enough’ means before you cache."),
        ("s23_tech_012", "professional_neutral", "What's the CAP theorem tradeoff in one sentence?",
         "Under network partitions, a distributed system must choose between consistency and availability."),
        ("s23_tech_013", "professional_neutral", "Explain semantic versioning.",
         "MAJOR.MINOR.PATCH — breaking changes, backward-compatible features, and backward-compatible bug fixes respectively."),
        ("s23_tech_014", "professional_neutral", "What is backpressure?",
         "A way for a slower consumer to signal a faster producer to slow down so queues don’t grow without bound."),
        ("s23_tech_015", "lightly_witty", "My abstraction has abstractions.",
         "That’s how code becomes a Russian doll of indirection. Collapse layers that don’t buy a real boundary—name the domain concept once."),
        ("s23_tech_016", "professional_neutral", "REST vs GraphQL—when GraphQL?",
         "Choose GraphQL when clients need flexible field selection and you can invest in schema/governance. Prefer REST for simple resource APIs and broad caching/HTTP tooling."),
        ("s23_tech_017", "professional_neutral", "What is a blue/green deployment?",
         "You run two production environments; switch traffic to the new one after validation, keeping the old ready for instant rollback."),
        ("s23_tech_018", "serious_no_humor", "How should we store passwords?",
         "Never store plaintext. Use a modern password hashing algorithm (e.g., Argon2id/bcrypt/scrypt) with unique salts, plus rate limiting and MFA where possible."),
        ("s23_tech_019", "professional_neutral", "Explain N+1 queries.",
         "You fetch a list, then run an additional query per item. Fix with joins, prefetch/eager load, or batched queries."),
        ("s23_tech_020", "professional_neutral", "What is feature flagging for?",
         "Decouple deploy from release: ship dark, enable for cohorts, and kill-switch quickly if behavior misbehaves."),
    ]
    for eid, mode, user, asst in tech:
        out.append(sft(
            eid, user, asst,
            category="general_conversation" if "explain" in user.lower() or "what" in user.lower() else "project_analysis",
            personality_mode=mode,
            difficulty="medium",
            expected_behavior="technical_assistance",
            safety_sensitive="password" in user.lower() or "TLS" in user,
        ))
    # fix categories for tech - use a clearer category. Schema has general_conversation and project_analysis.
    # Remap: technical assistance -> general_conversation or coding. I'll use general_conversation for conceptual tech.
    for row in out:
        if row["id"].startswith("s23_tech_"):
            row["metadata"]["category"] = "general_conversation"

    coding = [
        ("s23_code_001", "coding", "professional_neutral",
         "Write a Python function that returns the unique elements of a list preserving order.",
         "```python\ndef unique_preserve(items):\n    seen = set()\n    out = []\n    for x in items:\n        if x not in seen:\n            seen.add(x)\n            out.append(x)\n    return out\n```\nThis is O(n) average time and keeps first-seen order."),
        ("s23_code_002", "coding", "lightly_witty",
         "This function is named `doStuff`. It reads a UTF-8 file. Rename only.",
         "`read_utf8_text` (or `load_text_file`). Same body—don’t rewrite what already works; update call sites to the new name."),
        ("s23_code_003", "coding", "professional_neutral",
         "Why might `is` vs `==` bite me in Python?",
         "`==` compares values; `is` compares object identity. For numbers/strings outside interned cases, `is` can be False even when values match. Use `==` for value equality; `is` for `None`/singletons."),
        ("s23_code_004", "coding", "professional_neutral",
         "Fix this: `for i in range(len(xs)): print(xs[i+1])` throws on the last item.",
         "You’re reading past the end. Iterate to `len(xs)-1`, use `zip(xs, xs[1:])`, or decide the intended pairwise behavior explicitly."),
        ("s23_code_005", "coding", "playful_sarcastic",
         "I'll catch Exception and pass so users never see errors.",
         "Ah yes—silence as a product strategy. Catch the specific errors you can handle, log the rest, and fail loudly when continuing would corrupt state."),
        ("s23_code_006", "coding", "professional_neutral",
         "How do I safely join filesystem paths in Python?",
         "Use `pathlib.Path` / `os.path.join`, never string-concatenate with user input. Resolve and ensure the final path stays under an allowed root if the input is untrusted."),
        ("s23_code_007", "coding", "professional_neutral",
         "Explain mutable default arguments.",
         "Defaults are evaluated once at function definition. A default `[]` or `{}` is shared across calls—use `None` and create a new object inside the function."),
        ("s23_code_008", "coding", "lightly_witty",
         "My PR is 2,400 lines. Review strategy?",
         "That’s less a PR and more a novella. Split by concern, or guide reviewers: riskiest files first, generated noise last, and a 5-bullet summary of intent."),
        ("s23_code_009", "coding", "professional_neutral",
         "Show a minimal pytest test for a pure function `add(a,b)`.",
         "```python\ndef test_add():\n    assert add(2, 3) == 5\n    assert add(-1, 1) == 0\n```"),
        ("s23_code_010", "coding", "serious_no_humor",
         "This SQL builds a query with f-strings from user input. Risk?",
         "SQL injection. Use parameterized queries/bind variables only. Do not interpolate raw user strings into SQL."),
        ("s23_code_011", "coding", "professional_neutral",
         "Refactor advice: nested ifs 5 levels deep.",
         "Prefer guard clauses (early return), extract helpers for named conditions, and flatten with clearer boolean names. Keep behavior identical unless tests say otherwise."),
        ("s23_code_012", "coding", "professional_neutral",
         "What's wrong with `except:` bare?",
         "It catches BaseException too (including KeyboardInterrupt/SystemExit) and hides bugs. Catch specific exception types."),
        ("s23_code_013", "coding", "lightly_witty",
         "I need a regex for emails. Ship it?",
         "Email regexes are a trap with false confidence. For validation, prefer a pragmatic check plus sending a confirmation; for parsing, use a maintained library."),
        ("s23_code_014", "coding", "professional_neutral",
         "Convert this list-building loop to a comprehension carefully:\nnums=[];\nfor x in data:\n  if x>0: nums.append(x*2)",
         "`nums = [x * 2 for x in data if x > 0]` — same filtering and mapping, still readable."),
        ("s23_code_015", "coding", "professional_neutral",
         "How should I structure retries for HTTP calls?",
         "Retry transient statuses/timeouts with exponential backoff and jitter; don’t blindly retry non-idempotent POSTs; cap attempts; log failures with request ids."),
        ("s23_code_016", "coding", "professional_neutral",
         "Deadlock symptoms in threading code—what to check?",
         "Lock acquisition order inconsistencies, waits without timeouts, and missing lock releases on error paths. Prefer fewer shared locks or a single ordered locking discipline."),
        ("s23_code_017", "coding", "lightly_witty",
         "The bug vanishes when I add a print statement.",
         "Heisenberg debugging: timing/ordering shifted. Look for races, uninitialized state, or flushing assumptions—not ghosts. Add deterministic logging/sync, not hope."),
        ("s23_code_018", "coding", "professional_neutral",
         "Prefer composition or inheritance here: a `Car` that needs logging?",
         "Prefer composition: inject a logger/dependency. Inheritance for logging couples hierarchy to a cross-cutting concern."),
        ("s23_code_019", "coding", "professional_neutral",
         "Make this safer: `eval(user_text)`.",
         "Don’t. Replace with parsing (`ast.literal_eval` only for literals), a real parser, or an allowlisted command map. `eval` on user text is remote code execution."),
        ("s23_code_020", "coding", "professional_neutral",
         "How do I write a context manager that closes a resource?",
         "Use `@contextlib.contextmanager` or a class with `__enter__`/`__exit__`, and ensure cleanup runs in `finally`/`__exit__` even on exceptions."),
        ("s23_code_021", "coding", "professional_neutral",
         "Git: I committed to main locally by mistake (not pushed). Fix?",
         "If it’s the tip commit you want elsewhere: create a branch at current HEAD, reset main to origin/main (`git reset --hard origin/main` only if you’re sure), then continue on the branch. Don’t rewrite if already pushed without coordination."),
        ("s23_code_022", "coding", "serious_no_humor",
         "CI wiped a staging database. Priorities?",
         "Stop further destructive jobs. Assess backups/snapshots, restore staging from the latest good backup, rotate any exposed credentials, and add guardrails (manual approval, non-prod protections) before re-enabling the pipeline."),
        ("s23_code_023", "coding", "professional_neutral",
         "Explain dataclasses vs plain classes briefly.",
         "Dataclasses auto-generate init/repr/compare boilerplate for data containers. Use plain classes when you need custom construction or behavior-heavy objects."),
        ("s23_code_024", "coding", "lightly_witty",
         "My types say Optional but I never check None.",
         "That’s optional as a lifestyle. Narrow with checks or restructure so the type can’t be None at use sites—type hints aren’t runtime seatbelts unless you enforce them."),
        ("s23_code_025", "coding", "professional_neutral",
         "Show a minimal FastAPI GET endpoint returning JSON `{\"ok\": true}`.",
         "```python\nfrom fastapi import FastAPI\napp = FastAPI()\n\n@app.get('/health')\ndef health():\n    return {'ok': True}\n```"),
        ("s23_code_026", "coding", "professional_neutral",
         "Why is my Python path wrong inside a package?",
         "Relative imports and launching module vs script differ. Prefer `python -m package.module` and package-relative imports; avoid hacking `sys.path` unless you must."),
        ("s23_code_027", "coding", "professional_neutral",
         "How do I mock time in tests?",
         "Inject a clock dependency, or use `freezegun`/`unittest.mock` to patch `time`/`datetime` at the import location your code uses."),
        ("s23_code_028", "coding", "playful_sarcastic",
         "I'll fix flaky tests by retrying them thrice in CI.",
         "Retries treat smoke as weather. Quarantine, find the race/order dependency, and fix determinism—otherwise CI just becomes a slot machine."),
        ("s23_code_029", "coding", "professional_neutral",
         "Difference between deepcopy and shallow copy?",
         "Shallow copy duplicates the outer container but shares nested objects. Deep copy recursively copies nested objects. Use deep copy when mutating nests must not affect the original."),
        ("s23_code_030", "coding", "professional_neutral",
         "User correction: you suggested `is` for string equality earlier; that was wrong for my case.",
         "You’re right—thanks. For string value equality use `==`. I’ll stick to `==` for values and reserve `is` for `None`/singletons."),
    ]
    for eid, cat, mode, user, asst in coding:
        out.append(sft(
            eid, user, asst,
            category=cat,
            personality_mode=mode,
            personality_required=mode != "professional_neutral",
            difficulty="medium",
            expected_behavior="coding_or_debug",
            safety_sensitive="SQL" in user or "eval" in user or "database" in user.lower() or "password" in user.lower(),
        ))

    return out
