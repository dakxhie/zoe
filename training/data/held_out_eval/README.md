# Held-out evaluation

Primary set: `eval_sprint23.jsonl` (reviewed gold answers).

**Never** mix these prompts into `train/` or `validation/`.

The older `eval_seed_prompts.jsonl` stubs (if present) are deprecated—do not use for scoring.
