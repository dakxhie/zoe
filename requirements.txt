# Core runtime
typer
rich
python-dotenv

# Model inference (Colab GPU + local CUDA)
torch
transformers
huggingface_hub

# Planned for Sprint 1.2+ (memory, RAG, PDF ingestion)
sentence-transformers
chromadb
pypdf

# Utilities
requests
tqdm
