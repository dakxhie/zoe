zoe:
  profile: production
  mode: local

logging:
  level: INFO
  startup_verbose: false

cache:
  enabled: true

telemetry:
  enabled: true
  local_only: true

diagnostics:
  rich: false

startup:
  verify_folders: true
  initialize_plugins: true
  initialize_agents: true
